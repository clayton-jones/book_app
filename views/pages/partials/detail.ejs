<section>
  <div class="book">
    <img src=<%= book.image_url %> alt="book">
    <h2>
      <%= book.title %>
    </h2>
    <p><%= book.author %></p>
    <p><%= book.description %></p>
    <p><%= book.isbn%></p>
    <p>Bookshelf: <%= book.bookshelf%></p>
  </div>
</section>